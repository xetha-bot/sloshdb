{
    "private": true,
    "version": "1.0.0-0",
    "main": "dist/index.js",
    "scripts": {
        "start": "node . --trace-warnings",
        "prebuild": "rimraf dist",
        "build": "tsc --build",
        "v:patch": "npm version patch -m \"chore(version): %s\"",
        "v:minor": "npm version minor -m \"chore(version): %s\"",
        "v:major": "npm version major -m \"chore(version): %s\""
    },
    "dependencies": {
        "socket.io": "^3.1.0"
    },
    "devDependencies": {
        "@types/node": "^14.14.22",
        "@types/socket.io": "^2.1.13",
        "rimraf": "^3.0.2",
        "typescript": "^4.1.3"
    }
}
