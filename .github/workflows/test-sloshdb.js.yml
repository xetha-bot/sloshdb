name: test-sloshdb.js

on:
  push:
    branches: [development]
    paths:
      - sloshdb.js/**

jobs:
  build:
    runs-on: ubuntu-latest
    environment: Staging - sloshdb.js
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - working-directory: ./sloshdb.js
        run: |
          npm ci
          npm run build
          npm run test